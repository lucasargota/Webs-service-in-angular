import { CodeQrDirective } from './code-qr.directive';

describe('CodeQrDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeQrDirective();
    expect(directive).toBeTruthy();
  });
});
